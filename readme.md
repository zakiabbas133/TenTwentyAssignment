# TenTwentyAssignment

This is a simple **React Native Expo** project created as part of the TenTwenty assignment.

---

## 📋 Prerequisites

Make sure you have the following installed on your system:

- **Node.js** (LTS recommended)  
  👉 https://nodejs.org
- **npm** (comes with Node.js) or **yarn**
- **Expo CLI**
  
```bash
npm install -g expo-cli
```

🚀 How to Run the Project
1️⃣ Clone or Extract the Repository
After cloning or extracting the project, navigate to the project directory:

```bash
cd TenTwentyAssignment
```

⚠️ **Note**: The node_modules folder is NOT included in the repository.

2️⃣ Install Dependencies (node_modules)
Run the following command to install all required dependencies:

```bash
npm install
```

or if you use yarn:

```bash
yarn install
```

This will generate the node_modules folder.

3️⃣ Start the Expo Development Server

```bash
npx expo start
```

or

```bash
expo start
```

4️⃣ Run the App
Once Expo starts, you can run the app using any of the following options:

**📱 Expo Go App (Recommended)**

Install Expo Go from Play Store / App Store
Scan the QR code shown in the terminal or browser

**🤖 Android Emulator**

Press a in the terminal

**🍎 iOS Simulator (Mac only)**

Press i in the terminal

**🌐 Web**

Press w in the terminal

**🛠️ Tech Stack**

React Native

Expo

TypeScript
